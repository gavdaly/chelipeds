#!/usr/bin/env bash
set -e
dir="${XDG_PICTURES_DIR:-$HOME/Pictures}/Screenshots"
mkdir -p "$dir"
file="$dir/screenshot-$(date +%Y%m%d-%H%M%S).png"
if [ "${1:-}" = "area" ]; then grim -g "$(slurp)" "$file"; else grim "$file"; fi
notify-send "Screenshot saved" "$file" || true
echo "$file"
